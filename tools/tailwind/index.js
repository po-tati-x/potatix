/** @type {import('tailwindcss').Config} */
import animate from 'tailwindcss-animate';

const preset = {
  darkMode: 'class',
  theme: {
    extend: {},
  },
  plugins: [animate],
};

export default preset;